---
description: >-
  Example path-gated rule. Applies only to files matching the globs below.
  Duplicate this file per concern (e.g. tests.md, migrations.md, api.md).
globs:
  - "src/**/*"
---

# Example Path-Gated Rule

These instructions apply **only** when working on files that match `globs` above.

- <rule specific to this path, e.g. "All functions in src/ must have tests">
- <rule 2>
- <rule 3>

Keep rules narrow: a rule that always applies belongs in `CLAUDE.md` instead.
