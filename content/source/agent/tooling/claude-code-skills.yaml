id: agent.tooling.claude-code-skills
name: Agent Skills
category: tooling
domain: guidance
type: markdown
executable: false
version: 0.1.0
author: orkestra-core
description: >
  How to structure reusable expertise for coding agents.
compatibility:
  agents: [claude, codex, copilot, cursor, cline, aider]
  scopes: [global, project]
conflicts_with: []
requires: []
tags: [agents, skills, reusable-guidance]
content: |
  # Agent Skills

  - Store skills in the location expected by the active agent adapter.
  - Use a folder per skill.
  - Keep `SKILL.md` frontmatter plus instructions.
  - Use skills for repeatable, well-defined tasks.
  - Keep commands explicit and scoped to the task.
  - Check into Git when the knowledge is institutional.
