# Strict Coverage Enforcer

- Run `bin/check-coverage.sh` before declaring implementation work complete.
- Treat a failing coverage command as a blocking result.
- Do not claim completion until the project-specific coverage command passes.
