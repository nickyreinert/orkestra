# Context-Aware PR Generator

- Use `bin/create-pr.sh` instead of issuing an ad-hoc `gh pr create` command.
- Confirm the branch is pushed and the working tree is clean first.
- Provide a concise summary and testing notes; the script includes the repository template when present.
